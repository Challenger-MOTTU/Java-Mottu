CREATE TABLE patio(
                      id IDENTITY PRIMARY KEY,
                      nome VARCHAR(80) NOT NULL,
                      cidade VARCHAR(80) NOT NULL,
                      capacidade INT NOT NULL
);
