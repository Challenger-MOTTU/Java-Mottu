server.port=${PORT:8080}

spring.datasource.url=jdbc:h2:file:./data/motogrid;AUTO_SERVER=TRUE;MODE=LEGACY
spring.datasource.driverClassName=org.h2.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

management.endpoints.web.exposure.include=health,info

# ORACLE no deploy
oracle.datasource.url=${ORACLE_URL:jdbc:oracle:thin:@oracle.fiap.com.br:1521:orcl}
oracle.datasource.username=${ORACLE_USER:rm555427}
oracle.datasource.password=${ORACLE_PASS:060104}
oracle.datasource.driver-class-name=oracle.jdbc.OracleDriver

